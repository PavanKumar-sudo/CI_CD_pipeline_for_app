name: CI - Build & Deploy

on:
  push:
    branches: [main]
    paths:
      - 'backend/**'
      - 'frontend/**'
      - '.github/workflows/ci-build.yaml'

env:
  NODE_VERSION: 18
  DOCKER_IMAGE: ${{ secrets.DOCKERHUB_USERNAME }}/backend

jobs:
  test:
    runs-on: ubuntu-22.04  # ✅ Pinned to stable runner version
    steps:
      - uses: actions/checkout@v4.2.2

      - uses: actions/setup-node@v3.8.1
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          cache-dependency-path: backend/package-lock.json

      - name: Install backend deps
        working-directory: ./backend
        run: npm ci

      - name: Run unit tests
        working-directory: ./backend
        run: npm test

  docker:
    runs-on: ubuntu-22.04  # ✅ Pinned to stable runner version
    needs: test
    steps:
      - uses: actions/checkout@v4.2.2

      - uses: docker/login-action@v2.2.0
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and tag image
        run: |
          docker build -t $DOCKER_IMAGE:latest .
          docker tag $DOCKER_IMAGE:latest $DOCKER_IMAGE:${{ github.sha }}
        working-directory: ./backend # Assuming your Dockerfile is in the backend directory

      - name: Push image
        run: |
          docker push $DOCKER_IMAGE:latest
          docker push $DOCKER_IMAGE:${{ github.sha }}

      - name: Save image tag
        run: echo "${{ github.sha }}" > image-tag.txt

      - name: Confirm image tag file exists  # ✅ Debug step
        run: ls -l image-tag.txt

      - uses: actions/upload-artifact@v4
        with:
          name: image-tag
          path: image-tag.txt

  trivy-scan:
    runs-on: ubuntu-22.04  # ✅ Optional: consistency
    needs: docker
    steps:
      - name: Install Trivy
        run: |
          sudo apt-get update && sudo apt-get install -y wget
          wget https://github.com/aquasecurity/trivy/releases/latest/download/trivy_0.48.4_Linux-64bit.deb
          sudo dpkg -i trivy_0.48.4_Linux-64bit.deb

      - name: Scan for vulnerabilities
        run: trivy image --severity HIGH,CRITICAL $DOCKER_IMAGE:${{ github.sha }} > trivy-report.txt

      - name: Upload Trivy report to summary
        run: |
          echo "### Trivy Report" >> $GITHUB_STEP_SUMMARY
          cat trivy-report.txt >> $GITHUB_STEP_SUMMARY

  deploy:
    needs: trivy-scan
    uses: ./.github/workflows/deploy-app.yaml
    with:
      helm_release: loginapp
      image_tag: ${{ github.sha }}
    secrets: inherit

  notify:
    needs: deploy
    uses: ./.github/workflows/notify-discord.yaml
    with:
      status: ${{ needs.deploy.result }}
      scope: Build & Deploy
    secrets: inherit
